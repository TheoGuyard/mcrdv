on: push
name: Clippy

env:
  RUSTFLAGS: "-Dwarnings"

jobs:
  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run clippy
        run: cargo clippy --all-targets --all-features
