[package]
name = "scorpion"
version = "0.0.1"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
rand = { version = "0.8", features = ["small_rng"] }
nalgebra = "0.32"
roots = "0.0.8"

[profile.release]
debug = 1
