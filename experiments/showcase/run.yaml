name: showcase
description: Structure of a representative mission plan.

seed: 0

instance:
  name: iridium33
  num_chasers: 15
  max_load: 10
  max_time: 365d

solver:
  type: mcrdv
  args:
    transfer: {type: qlaw}
    schedule: {type: dp}
    sequence:
      type: hgs
      args: {preset: 5, max_time: 1h, verbose: true}
