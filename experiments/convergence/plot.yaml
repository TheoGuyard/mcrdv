source:
  folder: results
  select:
    instance.name: cosmos2251
    instance.num_chasers: 80
    instance.max_load: 10
    instance.max_time: 365d

figures:
  - name: convergence
    draw: convergence
    reduce: raw
    xlabel: running time [s]
    ylabel: mission cost
    xname: elapsed
